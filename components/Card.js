import React from "react";
const Card = ({wish}) => {
	const {title, desc, link, linkDesc = "Ссылка", img, price, status, date} = wish;
	console.log(img)
	return (
		<div>
		</div>
	)
}

export default Card
